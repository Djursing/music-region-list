<li id="<%= dom_id(zone) %>" class="flex items-center justify-between gap-4 px-4 py-2 text-sm">
  <span class="truncate"><%= zone.kommune_name %></span>
  <span class="shrink-0 text-neutral-400"><%= zone.artist.name %></span>
</li>
